# Ultralytics YOLO 🚀, AGPL-3.0 License
# YOLO11n_cbam_fixed.yaml

# Parameters
nc: 6 # number of classes
scales:
  n: [0.50, 0.25, 1024]
  s: [0.50, 0.50, 1024]
  m: [0.50, 1.00, 512]
  l: [1.00, 1.00, 512]
  x: [1.00, 1.50, 512]

# YOLO11n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]] # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]] # 1-P2/4
  - [-1, 2, C3k2, [256, False, 0.25]] # 2
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8
  - [-1, 2, C3k2, [512, False, 0.25]] # 4
  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]] # 8

  # 👇 插入层 (Index 9)
  - [-1, 1, CBAM, [1024]]    # 9. 新加CBAM (后续所有序号+1)

  - [-1, 1, SPPF, [1024, 5]] # 10 (原9)
  - [-1, 2, C2PSA, [1024]]   # 11 (原10)

# YOLO11n head
head:
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 12
  - [[-1, 6], 1, Concat, [1]] # 13. cat backbone P4 (序号6没变，因为在CBAM之前)
  - [-1, 2, C3k2, [512, False]] # 14 (原13)

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 15
  - [[-1, 4], 1, Concat, [1]] # 16. cat backbone P3 (序号4没变)
  - [-1, 2, C3k2, [256, False]] # 17 (原16. P3输出)

  - [-1, 1, Conv, [256, 3, 2]] # 18
  - [[-1, 14], 1, Concat, [1]] # 19. ⚠️注意：这里原来是13，现在改成14了！
  - [-1, 2, C3k2, [512, False]] # 20 (原19. P4输出)

  - [-1, 1, Conv, [512, 3, 2]] # 21
  - [[-1, 11], 1, Concat, [1]] # 22. ⚠️注意：这里原来是10，现在改成11了！
  - [-1, 2, C3k2, [1024, True]] # 23 (原22. P5输出)

  - [[17, 20, 23], 1, Detect, [nc]] # ⚠️注意：Detect层连接的三个头也都+1了